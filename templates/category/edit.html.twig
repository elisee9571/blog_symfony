{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}

{{ form_start(form_update) }}
    <div class="container">
        <div class="mb-3">
            {{ form_row(form_update.title) }}
        </div>
        <button type="submit" class="btn btn-primary">Modifier</button>
    </div>
{{ form_end(form_update) }}
{% endblock %}
